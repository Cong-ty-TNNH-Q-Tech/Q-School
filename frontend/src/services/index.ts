/**
 * Services barrel export — import tất cả service functions từ một chỗ.
 * Member dùng: import { apiClient } from '@/services'
 */
export { apiClient, mockApiResponse } from './apiClient'
export * from './mockData'
