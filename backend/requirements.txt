# FastAPI & Server
fastapi>=0.136.3
uvicorn[standard]>=0.47.0
python-multipart>=0.0.29
gunicorn>=21.2.0

# Database (PostgreSQL + pgvector)
SQLAlchemy>=2.0.49
alembic>=1.18.4
asyncpg>=0.31.0
psycopg2-binary>=2.9.12
pgvector>=0.4.2

# Storage & AI
boto3>=1.43.14         # Cho Cloudflare R2 / MinIO / S3
openai>=2.37.0          # Giao tiếp với vLLM server (OpenAI-compatible API)
httpx>=0.28.1          # Async HTTP client

# Background Jobs
celery>=5.6.3
redis>=5.0.1

# Security & Validation
pydantic[email]>=2.13.4
pydantic-settings>=2.0.3  # Load .env vào Settings class
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<5.0.0           # passlib compat — bcrypt 4.x cần truncate_error=False
python-jose[cryptography]>=3.3.0

# Utils
python-dotenv>=1.2.2

# Testing (dev/ci only — không cần trên production)
pytest>=7.4.0
pytest-asyncio>=1.3.0
httpx>=0.28.1
anyio>=4.13.0           # Backend cho pytest-asyncio
