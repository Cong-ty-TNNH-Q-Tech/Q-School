# FastAPI & Server
fastapi>=0.136.3
uvicorn[standard]>=0.49.0
python-multipart>=0.0.32
gunicorn>=26.0.0

# Database (PostgreSQL + pgvector)
SQLAlchemy>=2.0.51
alembic>=1.18.4
asyncpg>=0.31.0
psycopg2-binary>=2.9.12
pgvector>=0.4.2

# Storage & AI
boto3>=1.43.36         # Cho Cloudflare R2 / MinIO / S3
openai>=2.41.1          # Giao tiếp với vLLM server (OpenAI-compatible API)
httpx>=0.28.1          # Async HTTP client

# Background Jobs
celery>=5.6.3
redis>=8.0.0

# Security & Validation
pydantic[email]>=2.13.4
pydantic-settings>=2.14.2  # Load .env vào Settings class
pwdlib[bcrypt]>=0.2.1          # passlib replacement — tương thích bcrypt v5+
bcrypt>=5.0.0,<6.0.0          # bcrypt v5: enforce 72-byte limit (security improvement)
python-jose[cryptography]>=3.5.0

# Utils
python-dotenv>=1.2.2

# Testing (dev/ci only — không cần trên production)
pytest>=9.1.0
pytest-asyncio>=1.4.0
httpx>=0.28.1
anyio>=4.13.0           # Backend cho pytest-asyncio
